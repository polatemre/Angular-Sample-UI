import { FilterCapitalPipe } from './filter-capital.pipe';

describe('FilterCapitalPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCapitalPipe();
    expect(pipe).toBeTruthy();
  });
});
